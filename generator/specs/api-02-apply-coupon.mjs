// generator/specs/api-02-apply-coupon.mjs — POST /api/apply-coupon (API-02, Pool B, FR-09 +FR-08 +FR-10).
// Hanh vi thuc da xac nhan bang curl that (khong doan) — xem ai-audit/ai-audit-report.md.
//
// LUU Y THIET KE (rut kinh nghiem tu API-01):
//  - Moi chuoi state dung 1 don hang RIENG (tao qua checkout ngay trong chuoi), KHONG dung chung.
//  - Coupon VIP100 co gioi han 2 luot/nguoi TREN TOAN HE THONG (khong theo tung lan chay) — vi DB
//    duoc seed lai moi lan restart SUT nen an toan, nhung PHAI dat dung 1 lan duy nhat trong ca
//    collection cho tung nhanh (dung/khong dung/nguoi khac) de khong tu dam vao nhau.
//  - AI_MISTAKE co y: AI (luot dau) doc code `total_amount > coupon.min_order_amount` va SAO Y vao
//    expected (dung `>`), trong khi FR-09 C3 doi `>=`. Day la dung mo hinh "AI chep hanh vi code
//    roi dan nhan FR len" da canh bao o docs/03. Xem case TC-COUPON-004.

export const generated = [
  // ============================= DOMAIN (16 case) =============================
  {
    id: "TC-COUPON-001", technique: "Domain", partition: "hợp lệ điển hình — SAVE10 (percent) trên đơn > ngưỡng",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 500000, user_id: 999001 },
    expectedStatus: 200,
    expectedBody: "`discount_amount = 50000` (10% của 500.000), `final_amount = 450000` — theo công thức FR-09",
    basis: "FR-09: `discount_amount = total × discount_value / 100`", source: "AI",
    test: `pm.test("TC-COUPON-001: SAVE10 dung cong thuc FR-09", () => {\n  pm.response.to.have.status(200);\n  const b = pm.response.json();\n  pm.expect(b.discount_amount, "discount_amount").to.eql(50000);\n  pm.expect(b.final_amount, "final_amount").to.eql(450000);\n});`,
  },
  {
    id: "TC-COUPON-002", technique: "Domain", partition: "hợp lệ — BIGBUY (fixed) trên đơn > ngưỡng",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "BIGBUY", total_amount: 600000, user_id: 999002 },
    expectedStatus: 200, expectedBody: "`discount_amount = 50000` (fixed), `final_amount = 550000`",
    basis: "FR-09: loại fixed `discount_amount = discount_value`", source: "AI",
    test: `pm.test("TC-COUPON-002: BIGBUY fixed dung", () => {\n  pm.response.to.have.status(200);\n  const b = pm.response.json();\n  pm.expect(b.discount_amount).to.eql(50000);\n  pm.expect(b.final_amount).to.eql(550000);\n});`,
  },
  {
    id: "TC-COUPON-003", technique: "Domain", partition: "biên trên — `total_amount` LỚN HƠN 1đ so với `min_order_amount` (300.001)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 300001, user_id: 999003 },
    expectedStatus: 200, expectedBody: "200 — vượt ngưỡng dù chỉ 1đ",
    basis: "FR-09 C3", source: "AI",
    test: `pm.test("TC-COUPON-003: vua vuot nguong 1d", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-004", technique: "Domain", partition: "biên — `total_amount` ĐÚNG BẰNG `min_order_amount` (300.000) — AI_MISTAKE: chép hành vi code (`>`) thay vì đọc đúng FR-09 C3 (`>=`)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 300000, user_id: 999004 },
    expectedStatus: 400, expectedBody: "400 — AI (lượt đầu) đọc `if (total_amount > min_order_amount)` trong code rồi suy expected là 400 khi bằng nhau",
    basis: "hành vi code hiện tại (SAI — xem audit.md, FR-09 C3 ghi rõ `>=`)", source: "AI",
    test: `pm.test("TC-COUPON-004: bang dung nguong - AI_MISTAKE du doan 400", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-005", technique: "Domain", partition: "dưới ngưỡng 1đ (299.999)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 299999, user_id: 999005 },
    expectedStatus: 400, expectedBody: "400 `{error}` nói rõ chưa đủ ngưỡng",
    basis: "FR-09 C3", source: "AI",
    test: `pm.test("TC-COUPON-005: duoi nguong", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-006", technique: "Domain", partition: "mã EXPIRED — đã hết hạn (2020-01-01)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "EXPIRED", total_amount: 200000, user_id: 999006 },
    expectedStatus: 400, expectedBody: "400 `{error}` — hết hạn",
    basis: "FR-09 C2", source: "AI",
    test: `pm.test("TC-COUPON-006: het han", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-007", technique: "Domain", partition: "mã không tồn tại",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "KHONGTONTAI999", total_amount: 500000 }, expectedStatus: 404,
    expectedBody: "404 `{error}`", basis: "FR-09 C1", source: "AI",
    test: `pm.test("TC-COUPON-007: ma khong ton tai", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-008", technique: "Domain", partition: "mã rỗng",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "", total_amount: 500000 }, expectedStatus: 400,
    expectedBody: "400 `{error}` \"Vui lòng nhập mã giảm giá\"", basis: "spec §5.1", source: "AI",
    test: `pm.test("TC-COUPON-008: ma rong", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-009", technique: "Domain", partition: "thiếu hẳn field `code`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { total_amount: 500000 }, expectedStatus: 400,
    expectedBody: "400, KHÔNG 500", basis: "spec §5.1", source: "AI",
    test: `pm.test("TC-COUPON-009: thieu code", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-010", technique: "Domain", partition: "thiếu hẳn field `total_amount`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10" }, expectedStatus: 400,
    expectedBody: "400 hoặc lỗi được kiểm soát, KHÔNG 500", basis: "spec §5.1 im lặng — chỉ đảm bảo không 500", source: "AI",
    test: `pm.test("TC-COUPON-010: thieu total_amount", () => { pm.expect(pm.response.code, "khong duoc 500").to.not.eql(500); });`,
  },
  {
    id: "TC-COUPON-011", technique: "Domain", partition: "`total_amount` âm",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: -500000, user_id: 999011 }, expectedStatus: 400,
    expectedBody: "400 — đơn âm không hợp lệ, không được tính giảm giá", basis: "spec §5.1 im lặng — suy từ lẽ thường nghiệp vụ (đơn hàng không âm)", source: "AI",
    test: `pm.test("TC-COUPON-011: total_amount am", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-012", technique: "Domain", partition: "`total_amount` = 0",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 0, user_id: 999012 }, expectedStatus: 400,
    expectedBody: "400 — dưới ngưỡng", basis: "FR-09 C3", source: "AI",
    test: `pm.test("TC-COUPON-012: total_amount = 0", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-013", technique: "Domain", partition: "`total_amount` sai kiểu — chuỗi số `\"500000\"`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: "500000", user_id: 999013 }, expectedStatus: 200,
    expectedBody: "200 — JS ép kiểu chuỗi số khi so sánh `>`; kết quả tính toán vẫn phải đúng số",
    basis: "spec §5.1 im lặng — suy từ hành vi ép kiểu ngầm của JS", source: "AI",
    test: `pm.test("TC-COUPON-013: total_amount la chuoi so", () => {\n  pm.response.to.have.status(200);\n});`,
  },
  {
    id: "TC-COUPON-014", technique: "Domain", partition: "mã khác hoa/thường — `save10` (chữ thường)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "save10", total_amount: 500000, user_id: 999014 }, expectedStatus: 404,
    expectedBody: "404 — SQL so khớp `code = ?` phân biệt hoa/thường mặc định trong SQLite",
    basis: "spec §5.1 im lặng — suy từ hành vi so khớp mặc định của SQLite", source: "AI",
    test: `pm.test("TC-COUPON-014: ma chu thuong", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-015", technique: "Domain", partition: "mã có khoảng trắng đầu/cuối — ` SAVE10 `",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: " SAVE10 ", total_amount: 500000, user_id: 999015 }, expectedStatus: 404,
    expectedBody: "404 — không có `.trim()`, đặc tả không định nghĩa trim",
    basis: "spec §5.1 im lặng về trim", source: "AI",
    test: `pm.test("TC-COUPON-015: ma co khoang trang", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-016", technique: "Domain", partition: "`total_amount` số cực lớn (Number.MAX_SAFE_INTEGER)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 9007199254740991, user_id: 999016 }, expectedStatus: 200,
    expectedBody: "200, KHÔNG 500 — không giới hạn độ lớn trong đặc tả",
    basis: "spec §5.1 im lặng — chỉ đảm bảo không 500", source: "AI",
    test: `pm.test("TC-COUPON-016: total_amount cuc lon", () => { pm.expect(pm.response.code).to.not.eql(500); });`,
  },

  // ============================= STATE (14 case) — FR-10 order state machine =============================
  {
    id: "TC-COUPON-017", technique: "State", partition: "bước 1: thêm sản phẩm vào giỏ (setup luồng checkout)",
    method: "POST", path: "/api/cart", auth: "user",
    authHeader: "Bearer {{user_token}}",
    body: { id: 1, name: "iPhone 15 Pro Max", price: 30000000, quantity: 1 },
    expectedStatus: 200, expectedBody: "`{message}`", basis: "spec §4.2", source: "AI",
    test: `pm.test("TC-COUPON-017: them vao gio", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-018", technique: "State", partition: "bước 2: checkout tạo đơn — trạng thái khởi tạo phải là `pending`",
    method: "POST", path: "/api/checkout", auth: "user",
    authHeader: "Bearer {{user_token}}",
    body: { total_amount: 300000, shipping_address: "123 Le Loi" },
    expectedStatus: 200, expectedBody: "`{message, orderId}`, lưu `order_id`",
    basis: "spec §4.3 + FR-10 (trạng thái khởi tạo `pending`)", source: "AI",
    test: `pm.test("TC-COUPON-018: checkout tao don", () => {\n  pm.response.to.have.status(200);\n  const b = pm.response.json();\n  pm.expect(b.orderId).to.be.a("number");\n  pm.environment.set("order_id", b.orderId);\n});`,
  },
  {
    id: "TC-COUPON-019", technique: "State", partition: "bước 3: verify đơn vừa tạo có `status = pending`",
    method: "GET", path: "/api/orders/{{order_id}}", pathDesc: "/api/orders/{{order_id}}", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null,
    expectedStatus: 200, expectedBody: "`status = \"pending\"`",
    basis: "FR-10", source: "AI",
    test: `pm.test("TC-COUPON-019: don o trang thai pending", () => {\n  pm.response.to.have.status(200);\n  pm.expect(pm.response.json().status).to.eql("pending");\n});`,
  },
  {
    id: "TC-COUPON-020", technique: "State", partition: "bước 4: admin chuyển `pending → confirmed` (hợp lệ)",
    method: "PUT", path: "/api/admin/orders/{{order_id}}/status", pathDesc: "/api/admin/orders/{{order_id}}/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "confirmed" },
    expectedStatus: 200, expectedBody: "`{message}`", basis: "FR-10", source: "AI",
    test: `pm.test("TC-COUPON-020: pending -> confirmed hop le", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-021", technique: "State", partition: "bước 5: admin chuyển NHẢY CÓC `confirmed → delivered` — PHẢI bị chặn",
    method: "PUT", path: "/api/admin/orders/{{order_id}}/status", pathDesc: "/api/admin/orders/{{order_id}}/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "delivered" },
    expectedStatus: 400, expectedBody: "400 — FR-10 không cho nhảy cóc qua `shipping`",
    basis: "FR-10 sơ đồ chuyển trạng thái", source: "AI",
    test: `pm.test("TC-COUPON-021: nhay coc confirmed->delivered bi chan", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-022", technique: "State", partition: "bước 6: admin chuyển hợp lệ `confirmed → shipping`",
    method: "PUT", path: "/api/admin/orders/{{order_id}}/status", pathDesc: "/api/admin/orders/{{order_id}}/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "shipping" },
    expectedStatus: 200, expectedBody: "`{message}`", basis: "FR-10", source: "AI",
    test: `pm.test("TC-COUPON-022: confirmed -> shipping hop le", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-023", technique: "State", partition: "bước 7: user tự hủy đơn đang `shipping` — PHẢI bị chặn (FR-10 chỉ cho hủy khi pending/confirmed)",
    method: "PUT", path: "/api/orders/{{order_id}}/cancel", pathDesc: "/api/orders/{{order_id}}/cancel", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null,
    expectedStatus: 400, expectedBody: "400 — đơn đang `shipping` không được phép hủy",
    basis: "FR-10: chỉ hủy khi `pending`/`confirmed`", source: "AI",
    test: `pm.test("TC-COUPON-023: huy don dang shipping bi chan", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-024", technique: "State", partition: "bước 8: admin hoàn tất `shipping → delivered` (hợp lệ, trạng thái kết thúc)",
    method: "PUT", path: "/api/admin/orders/{{order_id}}/status", pathDesc: "/api/admin/orders/{{order_id}}/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "delivered" },
    expectedStatus: 200, expectedBody: "`{message}`", basis: "FR-10", source: "AI",
    test: `pm.test("TC-COUPON-024: shipping -> delivered hop le", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-025", technique: "State", partition: "bước 9: `delivered` là trạng thái KẾT THÚC — cố chuyển `delivered → shipping` phải bị chặn",
    method: "PUT", path: "/api/admin/orders/{{order_id}}/status", pathDesc: "/api/admin/orders/{{order_id}}/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "shipping" },
    expectedStatus: 400, expectedBody: "400 — `delivered` không được chuyển sang trạng thái nào khác",
    basis: "FR-10: 'delivered và canceled là trạng thái kết thúc'", source: "AI",
    test: `pm.test("TC-COUPON-025: delivered la trang thai ket thuc", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-026", technique: "State", partition: "đơn hàng khác — user tự hủy từ `pending` (hợp lệ)",
    method: "POST", path: "/api/checkout", auth: "user",
    authHeader: "Bearer {{user_token}}",
    body: { total_amount: 150000, shipping_address: "456 Nguyen Trai" },
    expectedStatus: 200, expectedBody: "`{message, orderId}` — lưu `order_id_2`",
    basis: "spec §4.3", source: "AI",
    test: `pm.test("TC-COUPON-026: tao don thu 2", () => {\n  pm.response.to.have.status(200);\n  pm.environment.set("order_id_2", pm.response.json().orderId);\n});`,
  },
  {
    id: "TC-COUPON-027", technique: "State", partition: "user hủy đơn thứ 2 đang `pending` — phải thành công",
    method: "PUT", path: "/api/orders/{{order_id_2}}/cancel", pathDesc: "/api/orders/{{order_id_2}}/cancel", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null,
    expectedStatus: 200, expectedBody: "`{message}`, status chuyển `canceled`",
    basis: "FR-10: hủy hợp lệ khi `pending`", source: "AI",
    test: `pm.test("TC-COUPON-027: huy don dang pending hop le", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-028", technique: "State", partition: "`canceled` là trạng thái KẾT THÚC — admin cố chuyển `canceled → delivered` phải bị chặn",
    method: "PUT", path: "/api/admin/orders/{{order_id_2}}/status", pathDesc: "/api/admin/orders/{{order_id_2}}/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "delivered" },
    expectedStatus: 400, expectedBody: "400 — `canceled` là trạng thái kết thúc, không được chuyển sang `delivered`",
    basis: "FR-10: 'delivered và canceled là trạng thái kết thúc'", source: "AI",
    test: `pm.test("TC-COUPON-028: canceled la trang thai ket thuc", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-029", technique: "State", partition: "hủy đơn không tồn tại",
    method: "PUT", path: "/api/orders/999999/cancel", pathDesc: "/api/orders/999999/cancel", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null,
    expectedStatus: 404, expectedBody: "404 `{error}`", basis: "spec §4.6", source: "AI",
    test: `pm.test("TC-COUPON-029: huy don khong ton tai", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-030", technique: "State", partition: "đổi trạng thái đơn không tồn tại",
    method: "PUT", path: "/api/admin/orders/999999/status", pathDesc: "/api/admin/orders/999999/status", auth: "admin",
    authHeader: "Bearer {{admin_token}}", body: { status: "confirmed" },
    expectedStatus: 404, expectedBody: "404 `{error}`", basis: "spec §6.2", source: "AI",
    test: `pm.test("TC-COUPON-030: doi trang thai don khong ton tai", () => { pm.response.to.have.status(404); });`,
  },

  // ============================= SECURITY (10 case) =============================
  {
    id: "TC-COUPON-031", technique: "Security", sec: "SEC-02",
    partition: "áp mã KHÔNG có token (FR-09 C4 đòi phải đăng nhập)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 500000 }, expectedStatus: 401,
    expectedBody: "401 — chưa đăng nhập", basis: "FR-09 C4: 'Người dùng phải có JWT Token hợp lệ' + SEC-02", source: "AI",
    test: `pm.test("TC-COUPON-031 SEC-02: khong token phai bi chan", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-COUPON-031b", technique: "Security", sec: "(chuẩn bị)",
    partition: "bước 0: tạo đơn RIÊNG cho case kiểm SEC-03 — cần đơn ở `pending` (chuyển đổi hợp lệ) để KHÔNG bị máy trạng thái chặn trước, tách bạch khỏi phép kiểm role",
    method: "POST", path: "/api/checkout", auth: "user", authHeader: "Bearer {{user_token}}",
    body: { total_amount: 120000, shipping_address: "don rieng cho SEC-03" },
    expectedStatus: 200, expectedBody: "`{message, orderId}` — lưu `order_id_3`",
    basis: "spec §4.3", source: "AI",
    test: `pm.test("TC-COUPON-031b: tao don rieng cho SEC-03", () => {\n  pm.response.to.have.status(200);\n  pm.environment.set("order_id_3", pm.response.json().orderId);\n});`,
  },
  {
    id: "TC-COUPON-032", technique: "Security", sec: "SEC-03",
    partition: "đổi trạng thái đơn bằng token USER THƯỜNG (không phải admin) — dùng `pending → confirmed`, MỘT chuyển đổi HỢP LỆ, để phép kiểm này chỉ đo role chứ không bị lẫn với lỗi máy trạng thái",
    method: "PUT", path: "/api/admin/orders/{{order_id_3}}/status", pathDesc: "/api/admin/orders/{{order_id_3}}/status", auth: "user",
    authHeader: "Bearer {{user_token}}", body: { status: "confirmed" },
    expectedStatus: 403, expectedBody: "403 — SEC-03 đòi kiểm `role='admin'` trong token, không chỉ có token; đây là chuyển đổi hợp lệ nên nếu qua được 400 thì chắc chắn là do thiếu kiểm role",
    basis: "SEC-03", source: "AI",
    test: `pm.test("TC-COUPON-032 SEC-03: user thuong khong duoc doi trang thai don", () => { pm.response.to.have.status(403); });`,
  },
  {
    id: "TC-COUPON-033", technique: "Security", sec: "(IDOR)",
    partition: "`GET /api/orders/:id` đọc đơn của NGƯỜI KHÁC — KHÔNG có token",
    method: "GET", path: "/api/orders/{{order_id}}", pathDesc: "/api/orders/{{order_id}}", auth: "không có header",
    body: null, expectedStatus: 401,
    expectedBody: "401 — xem chi tiết đơn hàng là dữ liệu cá nhân, phải cần xác thực",
    basis: "SEC-02 (suy từ nguyên tắc bảo vệ dữ liệu cá nhân — đặc tả §4.5 không ghi rõ nhưng cùng nhóm với các API đơn hàng khác đều yêu cầu Authorization)", source: "AI",
    test: `pm.test("TC-COUPON-033 IDOR: xem don khong token phai bi chan", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-COUPON-034", technique: "Security", sec: "(price tampering)",
    partition: "checkout với `total_amount` do client tự đặt — KHÔNG khớp giá trị giỏ hàng thật",
    method: "POST", path: "/api/checkout", auth: "user", authHeader: "Bearer {{user_token}}",
    body: { total_amount: 1, shipping_address: "gia tri gia mao" },
    expectedStatus: 400, expectedBody: "400 — server phải tự tính lại tổng tiền từ giỏ hàng, không tin `total_amount` client gửi",
    basis: "hệ quả bất lợi khi thiếu kiểm tra — suy từ nguyên tắc chung 'server không tin dữ liệu định giá từ client'", source: "AI",
    test: `pm.test("TC-COUPON-034: khong duoc tin total_amount tu client", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-035", technique: "Security", sec: "SEC-05",
    partition: "SQLi trong `code`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "' OR '1'='1", total_amount: 500000 }, expectedStatus: 404,
    expectedBody: "404 — coi là chuỗi tìm kiếm bình thường, không match, KHÔNG bypass", basis: "SEC-05", source: "AI",
    test: `pm.test("TC-COUPON-035 SEC-05: SQLi bi chan", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-036", technique: "Security", sec: "(IDOR quota)",
    partition: "dùng `user_id` của NGƯỜI KHÁC để né việc tiêu hạn mức của chính mình (mã VIP100, còn nguyên hạn mức của người bị mượn)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "VIP100", total_amount: 400000, user_id: "{{admin_id}}" }, expectedStatus: 400,
    expectedBody: "400 — `user_id` phải lấy từ token đã xác thực (SEC-02), không được nhận từ body client",
    basis: "FR-09 C4 (phải đăng nhập) — `user_id` client-controlled là hệ quả trực tiếp của việc thiếu C4", source: "AI",
    test: `pm.test("TC-COUPON-036 IDOR: user_id tu body la loi thiet ke", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-037", technique: "Security", sec: "(C5 bypass)",
    partition: "KHÔNG gửi `user_id` — hạn mức `max_uses_per_user` bị bỏ qua hoàn toàn (áp được vô hạn lần)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "VIP100", total_amount: 400000 }, expectedStatus: 400,
    expectedBody: "400 — nếu không xác định được người dùng thì không được áp dụng coupon giới hạn lượt dùng",
    basis: "FR-09 C5 (số lần dùng phải được kiểm với MỌI request, không phải chỉ khi client tự nguyện gửi user_id)", source: "AI",
    test: `pm.test("TC-COUPON-037: khong gui user_id khong duoc bo qua C5", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-038", technique: "Security", sec: "SEC-05",
    partition: "SQLi stacked query trong `code`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "x'; DROP TABLE coupons;--", total_amount: 500000 }, expectedStatus: 404,
    expectedBody: "404, KHÔNG 500, bảng `coupons` còn nguyên", basis: "SEC-05", source: "AI",
    test: `pm.test("TC-COUPON-038 SEC-05: stacked query khong lam sap bang", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-039", technique: "Security", sec: "(SEC-03, xác nhận thêm)",
    partition: "user thường gọi endpoint admin với chuyển đổi hợp lệ THỨ HAI (`confirmed → canceled` trên `order_id_3`, tiếp theo TC-COUPON-032) — xác nhận bug SEC-03 không phải ngẫu nhiên chỉ với 1 target",
    method: "PUT", path: "/api/admin/orders/{{order_id_3}}/status", pathDesc: "/api/admin/orders/{{order_id_3}}/status", auth: "user",
    authHeader: "Bearer {{user_token}}", body: { status: "canceled" },
    expectedStatus: 403, expectedBody: "403 — endpoint admin, user thường không được gọi dù trạng thái đích hợp lệ",
    basis: "SEC-03", source: "AI",
    test: `pm.test("TC-COUPON-039 SEC-03: user thuong khong duoc goi endpoint admin (target thu 2)", () => { pm.response.to.have.status(403); });`,
  },
  {
    id: "TC-COUPON-040", technique: "Security", sec: "(rò rỉ dữ liệu)",
    partition: "response `GET /api/orders/:id` (khi có token đúng chủ) KHÔNG được lộ field ngoài đặc tả",
    method: "GET", path: "/api/orders/{{order_id}}", pathDesc: "/api/orders/{{order_id}}", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null, expectedStatus: 200,
    expectedBody: "chỉ gồm id, user_id, total_amount, status, shipping_address, created_at",
    basis: "spec §4.5", source: "AI",
    test: `pm.test("TC-COUPON-040: don hang khong lo field thua", () => {\n  pm.response.to.have.status(200);\n  const allow = ["id","user_id","total_amount","status","shipping_address","created_at"];\n  Object.keys(pm.response.json()).forEach((k) => pm.expect(allow).to.include(k));\n});`,
  },

  // ============================= SCHEMA (9 case) =============================
  {
    id: "TC-COUPON-041", technique: "Schema", partition: "response thành công: đủ field & đúng kiểu",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 500000, user_id: 999041 }, expectedStatus: 200,
    expectedBody: "`success` boolean, `coupon_id` number, `discount_amount` number, `final_amount` number, `message` string",
    basis: "spec §5.1", source: "AI",
    test: `pm.test("TC-COUPON-041: schema thanh cong", () => {\n  const b = pm.response.json();\n  pm.expect(b.success).to.be.a("boolean");\n  pm.expect(b.coupon_id).to.be.a("number");\n  pm.expect(b.discount_amount).to.be.a("number");\n  pm.expect(b.final_amount).to.be.a("number");\n  pm.expect(b.message).to.be.a("string");\n});`,
  },
  {
    id: "TC-COUPON-042", technique: "Schema", partition: "`discount_amount` KHÔNG BAO GIỜ được âm",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 500000, user_id: 999042 }, expectedStatus: 200,
    expectedBody: "`discount_amount >= 0` — giảm giá không thể là số âm (tức phụ thu)",
    basis: "nguyên tắc nghiệp vụ cơ bản của giảm giá + hệ quả trực tiếp của công thức FR-09", source: "AI",
    test: `pm.test("TC-COUPON-042: discount_amount khong am", () => {\n  pm.expect(pm.response.json().discount_amount).to.be.at.least(0);\n});`,
  },
  {
    id: "TC-COUPON-043", technique: "Schema", partition: "`final_amount` KHÔNG BAO GIỜ được lớn hơn `total_amount`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "SAVE10", total_amount: 500000, user_id: 999043 }, expectedStatus: 200,
    expectedBody: "`final_amount <= total_amount` — sau giảm giá không thể tốn tiền hơn giá gốc",
    basis: "nguyên tắc nghiệp vụ cơ bản của giảm giá", source: "AI",
    test: `pm.test("TC-COUPON-043: final_amount khong duoc lon hon total_amount", () => {\n  const b = pm.response.json();\n  pm.expect(b.final_amount, "final_amount").to.be.at.most(500000);\n});`,
  },
  {
    id: "TC-COUPON-044", technique: "Schema", partition: "response lỗi 400/404: đúng shape `{error: string}`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "KHONGTONTAI888", total_amount: 500000 }, expectedStatus: 404,
    expectedBody: "chỉ `error` (string), Content-Type JSON", basis: "nguyên tắc API JSON nhất quán", source: "AI",
    test: `pm.test("TC-COUPON-044: schema loi", () => {\n  const b = pm.response.json();\n  pm.expect(b.error).to.be.a("string");\n  pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");\n});`,
  },
  {
    id: "TC-COUPON-045", technique: "Schema", partition: "`GET /api/orders/:id` không tồn tại → 404, không phải `200 {}`",
    method: "GET", path: "/api/orders/999999", pathDesc: "/api/orders/999999", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null, expectedStatus: 404,
    expectedBody: "404 `{error}`", basis: "spec §4.5", source: "AI",
    test: `pm.test("TC-COUPON-045: don khong ton tai - 404", () => { pm.response.to.have.status(404); });`,
  },
  {
    id: "TC-COUPON-046", technique: "Schema", partition: "`POST /api/checkout` response: `orderId` phải là number, không rỗng",
    method: "POST", path: "/api/checkout", auth: "user", authHeader: "Bearer {{user_token}}",
    body: { total_amount: 100000, shipping_address: "Test" }, expectedStatus: 200,
    expectedBody: "`orderId` number > 0", basis: "spec §4.3", source: "AI",
    test: `pm.test("TC-COUPON-046: orderId dung kieu", () => {\n  pm.expect(pm.response.json().orderId).to.be.a("number").and.above(0);\n});`,
  },
  {
    id: "TC-COUPON-047", technique: "Schema", partition: "method không hỗ trợ — `GET /api/apply-coupon`",
    method: "GET", path: "/api/apply-coupon", auth: "không có header", body: null,
    expectedStatus: 404, expectedBody: "404 (Express mặc định)", basis: "spec §5.1 chỉ định nghĩa POST", source: "AI",
    test: `pm.test("TC-COUPON-047: GET khong ho tro", () => { pm.response.to.have.status(404); });`,
  },
];

export const own = [
  {
    id: "TC-COUPON-101", technique: "Security", sec: "(price tampering — chuỗi 2 bước)",
    partition: "SV: tạo giỏ hàng thật giá trị LỚN rồi checkout với `total_amount` GIẢ MẠO thấp hơn nhiều — verify bằng GET đơn xem giá trị lưu có đúng bằng giá trị GIẢ hay bị server tính lại",
    method: "POST", path: "/api/checkout", auth: "user", authHeader: "Bearer {{user_token}}",
    bodyDesc: "giỏ có iPhone 30.000.000đ, nhưng checkout gửi `total_amount: 1`",
    body: { total_amount: 1, shipping_address: "gia mao" },
    expectedStatus: 400, expectedBody: "400 — KHÔNG được tạo đơn với giá trị không khớp giỏ hàng thật",
    basis: "hệ quả nghiêm trọng của thiếu server-side price validation — phát hiện khi đọc code checkout không hề gọi `GET /api/cart`", source: "SV",
    test: `pm.test("TC-COUPON-101 SV: checkout gia mao gia phai bi chan", () => { pm.response.to.have.status(400); });`,
    why: "AI (bước 4b) sinh case SQLi/auth theo đúng khuôn 'security testing' quen thuộc — nó không tự nối 2 endpoint khác nhau (`cart` và `checkout`) lại để phát hiện ra rằng SERVER KHÔNG BAO GIỜ ĐỌC LẠI GIỎ HÀNG khi tạo đơn. Đây là lỗ hổng nghiệp vụ xuyên-API, cần đọc cả 2 handler cùng lúc mới thấy.",
    why_group: "characteristics of the API",
  },
  {
    id: "TC-COUPON-102", technique: "State", partition: "SV bước 1/5: áp `VIP100` LẦN 1 với `user_id` THẬT (hợp lệ)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "VIP100", total_amount: 400000, user_id: "{{user_id}}" },
    expectedStatus: 200, expectedBody: "200 — lượt dùng thứ 1/2",
    basis: "FR-09 C5", source: "SV",
    test: `pm.test("TC-COUPON-102 buoc 1: VIP100 lan 1 hop le", () => { pm.response.to.have.status(200); });`,
    why: "AI chỉ sinh case về hạn mức ở mức KHÁI NIỆM (thiếu user_id → bỏ qua kiểm tra), không tự dựng ĐỦ MỘT CHUỖI 5 request thật (2 lần apply + 2 lần ghi usage + 1 lần kiểm chặn) cần thiết để CHỨNG MINH khi dùng đúng cách C5 vẫn hoạt động — case đối chứng để không nhận nhầm 'C5 lúc nào cũng hỏng'.",
    why_group: "model limitations",
  },
  {
    id: "TC-COUPON-102b", technique: "State", partition: "SV bước 2/5: ghi nhận lượt dùng #1 (mô phỏng bước checkout thành công gọi `coupon-usage`)",
    method: "POST", path: "/api/coupon-usage", auth: "user", authHeader: "Bearer {{user_token}}",
    body: { coupon_id: 3 }, expectedStatus: 200, expectedBody: "`{message}`",
    basis: "spec §5 (ngụ ý — API ghi lượt dùng tồn tại riêng)", source: "SV",
    test: `pm.test("TC-COUPON-102b: ghi usage lan 1", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-102c", technique: "State", partition: "SV bước 3/5: áp `VIP100` LẦN 2 (vẫn còn hạn mức 2/2)",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "VIP100", total_amount: 400000, user_id: "{{user_id}}" },
    expectedStatus: 200, expectedBody: "200 — lượt dùng thứ 2/2",
    basis: "FR-09 C5", source: "SV",
    test: `pm.test("TC-COUPON-102c: VIP100 lan 2 hop le", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-102d", technique: "State", partition: "SV bước 4/5: ghi nhận lượt dùng #2",
    method: "POST", path: "/api/coupon-usage", auth: "user", authHeader: "Bearer {{user_token}}",
    body: { coupon_id: 3 }, expectedStatus: 200, expectedBody: "`{message}`",
    basis: "spec §5", source: "SV",
    test: `pm.test("TC-COUPON-102d: ghi usage lan 2", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-COUPON-102e", technique: "State", partition: "SV bước 5/5: áp `VIP100` LẦN 3 với `user_id` THẬT — đã dùng đủ 2/2, PHẢI bị chặn",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "VIP100", total_amount: 400000, user_id: "{{user_id}}" },
    expectedStatus: 400, expectedBody: "400 — đã đạt giới hạn 2 lượt (C5), khi `user_id` khớp đúng người dùng thật (đối chứng: dùng đúng cách thì C5 hoạt động — khác TC-COUPON-037 kiểm việc BỎ QUA C5 khi thiếu `user_id`)",
    basis: "FR-09 C5", source: "SV",
    test: `pm.test("TC-COUPON-102e: VIP100 lan 3 (dung user that) bi chan", () => { pm.response.to.have.status(400); });`,
  },
  {
    id: "TC-COUPON-103", technique: "Domain", partition: "SV: mã `type` không hợp lệ trong DB (giả lập bằng cách kiểm response khi loại khác `percent`/`fixed`) — kiểm `discount_amount` không bị `NaN`/`undefined`",
    method: "POST", path: "/api/apply-coupon", auth: "không có header",
    body: { code: "BIGBUY", total_amount: 1000000, user_id: 999103 },
    expectedStatus: 200, expectedBody: "`discount_amount` phải là number hợp lệ, không phải `NaN`",
    basis: "spec §5.1 im lặng — kiểm tính toàn vẹn số học của công thức khi loại là `fixed`", source: "SV",
    test: `pm.test("TC-COUPON-103 SV: discount_amount khong duoc NaN", () => {\n  pm.response.to.have.status(200);\n  pm.expect(pm.response.json().discount_amount).to.not.be.NaN;\n});`,
    why: "AI kiểm shape/kiểu dữ liệu (\"discount_amount là number\") nhưng `NaN` VẪN có `typeof === \"number\"` trong JavaScript — một assertion `to.be.a(\"number\")` không bắt được `NaN`. Đây là giới hạn của AI khi sinh assertion: nó không biết `NaN` là trường hợp đặc biệt cần kiểm riêng.",
    why_group: "model limitations",
  },
  {
    id: "TC-COUPON-104", technique: "State", partition: "SV: order state — hủy đơn 2 LẦN liên tiếp (đơn đã `canceled` rồi hủy lại) — phải bị chặn ở lần 2",
    method: "PUT", path: "/api/orders/{{order_id_2}}/cancel", pathDesc: "/api/orders/{{order_id_2}}/cancel", auth: "user",
    authHeader: "Bearer {{user_token}}", body: null,
    expectedStatus: 400, expectedBody: "400 — đơn đã `canceled` rồi thì không được hủy lại (đã là trạng thái kết thúc)",
    basis: "FR-10: 'canceled là trạng thái kết thúc'", source: "SV",
    test: `pm.test("TC-COUPON-104 SV: huy don da canceled lan 2 bi chan", () => { pm.response.to.have.status(400); });`,
    why: "AI sinh case cho TỪNG cặp chuyển trạng thái đơn lẻ (vd `shipping->cancel`) theo đúng bảng liệt kê từ sơ đồ FR-10, nhưng không tự nghĩ tới trường hợp GỌI LẠI CHÍNH THAO TÁC ĐÃ THÀNH CÔNG một lần nữa — đây là kiểu lỗi 'idempotency/double-submit' mà chỉ thấy khi tự đặt câu hỏi 'điều gì xảy ra nếu người dùng bấm nút Hủy hai lần liên tiếp'.",
    why_group: "prompt quality",
  },
  {
    id: "TC-COUPON-105", technique: "Security", sec: "(kiểm đối chứng — hành vi ĐÚNG)",
    partition: "SV: user KHÁC (admin, không phải chủ đơn) gọi `PUT /orders/:id/cancel` cho đơn của người khác — endpoint này CÓ lọc `WHERE id=? AND user_id=?` theo token, nên phải KHÔNG tìm thấy đơn (404), không lộ được là đơn có tồn tại hay không",
    method: "PUT", path: "/api/orders/{{order_id_2}}/cancel", pathDesc: "/api/orders/{{order_id_2}}/cancel", auth: "admin (không phải chủ đơn)",
    authHeader: "Bearer {{admin_token}}", body: null,
    expectedStatus: 404, expectedBody: "404 `{error:\"Order not found\"}` — ownership được lọc đúng theo `req.user.id`, không lộ rằng đơn thực ra tồn tại (thuộc về người khác)",
    basis: "spec §4.6 + suy từ mã nguồn: `SELECT * FROM orders WHERE id=? AND user_id=?` dùng `req.user.id` — đây là **hành vi phòng thủ đúng đắn**, ghi lại làm case đối chứng để không nhận vơ mọi thứ đều là bug", source: "SV",
    test: `pm.test("TC-COUPON-105 SV: nguoi khac khong huy duoc don, khong lo ton tai (404)", () => {\n  pm.response.to.have.status(404);\n});`,
    why: "AI kiểm SEC-03 theo chiều 'quyền thấp gọi API cao hơn' (user gọi endpoint admin), nhưng không tự đặt câu hỏi CHIỀU NGƯỢC LẠI: 'endpoint dành cho user có bị người khác (kể cả admin) thao tác nhầm lên dữ liệu của ai đó không'. Case này ban đầu bị đặt SAI kỳ vọng (403) — audit đã sửa lại thành 404 sau khi đối chiếu với đúng câu lệnh SQL trong `server.js`, và hóa ra đây là một trong số ít chỗ SUT làm ĐÚNG. Giữ lại làm case đối chứng: không phải mọi endpoint đều có lỗ hổng.",
    why_group: "model limitations",
  },
];
