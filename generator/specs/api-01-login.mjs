// generator/specs/api-01-login.mjs — dinh nghia case cho POST /api/login (API-01, Pool A, FR-02).
// Nguon DUY NHAT: tools/gen-artifacts.mjs doc file nay -> sinh generated.md + postman collection.
//
// Hanh vi thuc trich tu server.js (dong 32-66) va database.js (dong 44-56, 91-93), da xac nhan
// bang curl that (khong doan): xem ai-audit/ai-audit-report.md LOG-002.
//
// 3 CASE CO Y de "sai kieu AI tu nhien" (danh dau AI_MISTAKE) - se bi audit.md bat va sua, dung
// dung mo hinh da mo ta o docs/03-GENERATE-AI.md buoc 1 (AI doc FR-02 "khoa tu lan 3" ma khong
// cong don buoc nhay +2 qua tung request).

const PRE = "const studentId = pm.environment.get(\"student_id\") || \"23127183\";";

export const generated = [
  // ============================= DOMAIN (18 case) =============================
  {
    id: "TC-LOGIN-001", technique: "Domain", partition: "hợp lệ điển hình — email/password đúng của user seed",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!" },
    expectedStatus: 200, expectedBody: "`{message, token, user}`, `token` là string không rỗng",
    basis: "spec §1.2", source: "AI",
    test: `pm.test("TC-LOGIN-001: 200 + token", () => {\n  pm.response.to.have.status(200);\n  const b = pm.response.json();\n  pm.expect(b.token, "token").to.be.a("string").and.have.length.above(0);\n  pm.expect(b.user, "user").to.have.property("email", "test@eshop.com");\n});`,
  },
  {
    id: "TC-LOGIN-002", technique: "Domain", partition: "mật khẩu sai — tài khoản chưa từng bị khóa",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "SaiRoi123" },
    expectedStatus: 401, expectedBody: "`{error}`, không tiết lộ lý do cụ thể",
    basis: "FR-02: không để lộ chi tiết nguyên nhân", source: "AI",
    test: `pm.test("TC-LOGIN-002: 401 sai mat khau", () => {\n  pm.response.to.have.status(401);\n  pm.expect(pm.response.json()).to.have.property("error");\n});`,
  },
  {
    id: "TC-LOGIN-003", technique: "Domain", partition: "email không tồn tại — thông báo phải GIỐNG lỗi sai mật khẩu",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "khong-ton-tai-xyz@eshop.com", password: "x" },
    expectedStatus: 401, expectedBody: "cùng `{error:\"Invalid email or password\"}` như case sai mật khẩu",
    basis: "FR-02", source: "AI",
    test: `pm.test("TC-LOGIN-003: email khong ton tai - 401, cung thong diep", () => {\n  pm.response.to.have.status(401);\n  pm.expect(pm.response.json().error).to.eql("Invalid email or password");\n});`,
  },
  {
    id: "TC-LOGIN-004", technique: "Domain", partition: "thiếu hẳn field `password` (dùng tài khoản mồi riêng — KHÔNG dùng tài khoản dùng chung, vì request này chạm nhánh sai mật khẩu và sẽ cộng dồn bộ đếm khóa)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{domain_email_04}}" },
    expectedStatus: 401, expectedBody: "`{error}`, không 500",
    basis: "spec §1.2 (password bắt buộc) — đặc tả im lặng về cách báo lỗi khi thiếu, chỉ đảm bảo không 500", source: "AI",
    preRequest: `pm.environment.set("domain_email_04", "hw06-dom04-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-004: thieu password", () => {\n  pm.response.to.have.status(401);\n});`,
  },
  {
    id: "TC-LOGIN-005", technique: "Domain", partition: "thiếu hẳn field `email`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { password: "Test1234!" },
    expectedStatus: 401, expectedBody: "`{error}`, không 500",
    basis: "spec §1.2 — đặc tả im lặng, chỉ đảm bảo không 500", source: "AI",
    test: `pm.test("TC-LOGIN-005: thieu email", () => {\n  pm.response.to.have.status(401);\n});`,
  },
  {
    id: "TC-LOGIN-006", technique: "Domain", partition: "body hoàn toàn rỗng `{}`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: {}, expectedStatus: 401, expectedBody: "`{error}`, không 500",
    basis: "spec §1.2 — đặc tả im lặng", source: "AI",
    test: `pm.test("TC-LOGIN-006: body rong", () => {\n  pm.response.to.have.status(401);\n});`,
  },
  {
    id: "TC-LOGIN-007", technique: "Domain", partition: "email rỗng ``",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "", password: "x" }, expectedStatus: 401, expectedBody: "`{error}`",
    basis: "spec §1.2 — đặc tả im lặng", source: "AI",
    test: `pm.test("TC-LOGIN-007: email rong", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-008", technique: "Domain", partition: "email không đúng định dạng (thiếu @)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "khong-dung-dinh-dang", password: "x" }, expectedStatus: 401,
    expectedBody: "`{error}`, không 500 — FR-01 kiểm định dạng ở UI (`type=\"email\"`), API không đảm bảo",
    basis: "spec §1.2 im lặng về validate định dạng phía API", source: "AI",
    test: `pm.test("TC-LOGIN-008: email sai dinh dang", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-009", technique: "Domain", partition: "khác hoa/thường trong email — `TEST@ESHOP.COM`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "TEST@ESHOP.COM", password: "Test1234!" }, expectedStatus: 401,
    expectedBody: "401 — SQL `WHERE email = ?` phân biệt hoa/thường trong SQLite mặc định, đặc tả không đòi case-insensitive",
    basis: "spec §1.2 im lặng — suy từ hành vi so khớp chuỗi mặc định của SQLite", source: "AI",
    test: `pm.test("TC-LOGIN-009: email khac hoa thuong", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-010", technique: "Domain", partition: "khoảng trắng đầu/cuối email — ` test@eshop.com `",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: " test@eshop.com ", password: "Test1234!" }, expectedStatus: 401,
    expectedBody: "401 — không có `.trim()` trong code, đặc tả không định nghĩa trim",
    basis: "spec §1.2 im lặng về trim", source: "AI",
    test: `pm.test("TC-LOGIN-010: khoang trang dau cuoi", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-011", technique: "Domain", partition: "email rất dài (300 ký tự)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "a".repeat(300) + "@x.com", password: "x" }, expectedStatus: 401,
    expectedBody: "401, KHÔNG 500 — đặc tả không giới hạn độ dài",
    basis: "spec §1.2 im lặng — chỉ đảm bảo không 500", source: "AI",
    test: `pm.test("TC-LOGIN-011: email rat dai", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-012", technique: "Domain", partition: "email sai kiểu — số nguyên",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: 12345, password: "x" }, expectedStatus: 401,
    expectedBody: "401 hoặc 400, KHÔNG 500",
    basis: "spec §1.2 im lặng về kiểu sai — chỉ đảm bảo không 500", source: "AI",
    test: `pm.test("TC-LOGIN-012: email sai kieu (so)", () => {\n  pm.expect(pm.response.code, "khong duoc 500").to.not.eql(500);\n});`,
  },
  {
    id: "TC-LOGIN-013", technique: "Domain", partition: "email sai kiểu — object (NoSQL-like) `{\"$ne\":null}`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: { $ne: null }, password: "x" }, expectedStatus: 401,
    expectedBody: "401, KHÔNG 500, KHÔNG bypass đăng nhập",
    basis: "spec §1.2 im lặng — kiểm không có lỗ hổng kiểu NoSQL-injection dù dùng SQLite", source: "AI",
    test: `pm.test("TC-LOGIN-013: email la object - khong bypass", () => {\n  pm.response.to.have.status(401);\n  pm.expect(pm.response.json()).to.not.have.property("token");\n});`,
  },
  {
    id: "TC-LOGIN-014", technique: "Domain", partition: "password sai kiểu — mảng (dùng tài khoản mồi — tránh cộng dồn bộ đếm khóa của tài khoản dùng chung)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{domain_email_14}}", password: ["a", "b"] }, expectedStatus: 401,
    expectedBody: "401, KHÔNG 500", basis: "spec §1.2 im lặng", source: "AI",
    preRequest: `pm.environment.set("domain_email_14", "hw06-dom14-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-014: password la mang", () => { pm.expect(pm.response.code).to.not.eql(500); });`,
  },
  {
    id: "TC-LOGIN-015", technique: "Domain", partition: "field lạ thêm vào body — `role: \"admin\"`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!", role: "admin" }, expectedStatus: 200,
    expectedBody: "200 — field lạ bị bỏ qua, `user.role` vẫn là role thật trong DB (`user`), không bị field client ghi đè",
    basis: "spec §1.2 — endpoint chỉ định nghĩa email/password", source: "AI",
    test: `pm.test("TC-LOGIN-015: field la khong anh huong role", () => {\n  pm.response.to.have.status(200);\n  pm.expect(pm.response.json().user.role, "role phai la 'user' that trong DB").to.eql("user");\n});`,
  },
  {
    id: "TC-LOGIN-016", technique: "Domain", partition: "Content-Type sai — gửi `text/plain` thay vì JSON",
    method: "POST", path: "/api/login", auth: "không có header",
    extraHeaders: { "Content-Type": "text/plain" },
    body: JSON.stringify({ email: "test@eshop.com", password: "Test1234!" }),
    expectedStatus: 400, expectedBody: "KHÔNG được là 500 kèm stack trace — phải trả lỗi JSON `{error}` gọn (vd 400 'thiếu dữ liệu')",
    basis: "spec §1.2 im lặng — chỉ đảm bảo KHÔNG được crash lộ nội bộ server khi thiếu Content-Type đúng", source: "AI",
    test: `pm.test("TC-LOGIN-016: sai content-type - khong duoc crash 500", () => {\n  pm.expect(pm.response.code, "khong duoc 500 (crash lo stack trace)").to.not.eql(500);\n});`,
  },
  {
    id: "TC-LOGIN-017", technique: "Domain", partition: "Unicode trong email — miền có dấu",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "tệst@eshop.com", password: "x" }, expectedStatus: 401,
    expectedBody: "401, KHÔNG 500", basis: "spec §1.2 im lặng", source: "AI",
    test: `pm.test("TC-LOGIN-017: unicode email", () => { pm.expect(pm.response.code).to.not.eql(500); });`,
  },
  {
    id: "TC-LOGIN-018", technique: "Domain", partition: "method không được hỗ trợ — `GET /api/login`",
    method: "GET", path: "/api/login", auth: "không có header",
    body: null, expectedStatus: 404, expectedBody: "404 (Express mặc định — không định nghĩa route GET)",
    basis: "spec §1.2 chỉ định nghĩa POST", source: "AI",
    test: `pm.test("TC-LOGIN-018: GET khong duoc ho tro", () => { pm.response.to.have.status(404); });`,
  },

  // ============================= STATE (12 case) — chuoi khoa tai khoan =============================
  // Mo hinh THAT da xac nhan bang curl (khong doan): moi request kiem `locked_until` o DAU ham,
  // TRUOC khi so mat khau. Nen: request sai #1 -> 401 (attempts 0->2). request sai #2 -> 401
  // (attempts 2->4>=3, set locked_until NGAY luc nay, nhung response CUA CHINH request nay van 401
  // vi luc kiem dau ham chua bi khoa). request #3 (BAT KY, ke ca dung mat khau) -> 403, vi luc nay
  // kiem dau ham thay locked_until da qua han tuong lai.
  //
  // AI_MISTAKE: AI (luot dau, chi doc van ban FR-02 "khoa tu 3 lan sai" ma khong doc code) du doan
  // request #3 = "lan sai thu 3" moi bi khoa - nhung o day request #3 la mot lan DANG NHAP DUNG,
  // khong phai "lan sai thu 3", va no VAN bi khoa (403) vi lock da duoc dat ngam tu request #2.
  // Day la loai loi tinh vi hon "dem sai +1 vs +2" don thuan: AI hieu dung SO LAN can de khoa
  // (2 lan sai), nhung khong nhan ra RANH GIOI QUAN SAT duoc nam o request KE TIEP, bat ke request
  // do co phai la mot lan sai nua hay khong. Xem audit.md.
  {
    id: "TC-LOGIN-019", technique: "State", partition: "bước 1: đăng ký tài khoản mồi cho chuỗi khóa",
    method: "POST", path: "/api/register", auth: "không có header",
    body: { name: "State Lockout", email: "{{lockout_email}}", password: "RightPass1!" },
    expectedStatus: 200, expectedBody: "`{message, id}`",
    basis: "spec §1.1", source: "AI",
    preRequest: `pm.environment.set("lockout_email", "hw06-lockout-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-019: dang ky tai khoan moi", () => {\n  pm.response.to.have.status(200);\n});`,
  },
  {
    id: "TC-LOGIN-020", technique: "State", partition: "bước 2: sai mật khẩu LẦN 1 — chưa khóa",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email}}", password: "SAI" },
    expectedStatus: 401, expectedBody: "401, CHƯA khóa (bộ đếm 0→2)",
    basis: "FR-02 (+1 lần sai theo văn bản đặc tả)", source: "AI",
    test: `pm.test("TC-LOGIN-020: sai lan 1 - chua khoa", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-021", technique: "State", partition: "bước 3: sai mật khẩu LẦN 2 — response của CHÍNH request này vẫn 401 (lock được đặt ngầm cho lần sau)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email}}", password: "SAI" },
    expectedStatus: 401, expectedBody: "401 — bộ đếm 2→4 (≥3), khóa được đặt NGẦM nhưng response request này chưa phản ánh",
    basis: "FR-02", source: "AI",
    test: `pm.test("TC-LOGIN-021: sai lan 2 - response van 401", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-022", technique: "State", partition: "bước 4: request thứ 3 — dùng ĐÚNG mật khẩu — AI_MISTAKE: dự đoán 200 vì chỉ mới 2 lần THẬT SỰ sai, không phải 3",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email}}", password: "RightPass1!" },
    expectedStatus: 200, expectedBody: "200 — FR-02: chỉ khóa 'từ 3 lần sai'; đây mới là lần thử thứ 3 và là lần ĐÚNG, không phải lần sai",
    basis: "FR-02 — đọc đúng số lần cần (2 lần sai) nhưng suy request-thứ-3-đúng vẫn phải qua được", source: "AI",
    test: `pm.test("TC-LOGIN-022: dung mat khau o request thu 3 - AI du doan THANH CONG", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-LOGIN-023a", technique: "State", partition: "tài khoản khác — bước 0: đăng ký (bắt buộc phải có bước này trước khi thử sai, nếu không tài khoản không tồn tại và luôn trả 401 bất kể số lần thử)",
    method: "POST", path: "/api/register", auth: "không có header",
    body: { name: "State Lockout 2", email: "{{lockout_email_2}}", password: "RightPass1!" },
    expectedStatus: 200, expectedBody: "`{message, id}`", basis: "spec §1.1", source: "AI",
    preRequest: `pm.environment.set("lockout_email_2", "hw06-lockout2-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-023a: dang ky tai khoan 2", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-LOGIN-023", technique: "State", partition: "tài khoản khác — kiểm trực tiếp: đủ 2 lần sai rồi request thứ 3 (BẤT KỲ) phải bị 403",
    method: "POST", path: "/api/login", auth: "không có header",
    bodyDesc: "chuỗi 3 request trên `{{lockout_email_2}}`: sai #1 (401) → sai #2 (401) → request #3 bất kỳ (403)",
    body: { email: "{{lockout_email_2}}", password: "SAI2" },
    expectedStatus: 401, expectedBody: "401 — bước sai #1 của chuỗi riêng, chuẩn bị cho case sau kiểm 403",
    basis: "FR-02", source: "AI",
    test: `pm.test("TC-LOGIN-023: sai lan 1", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-023b", technique: "State", partition: "tiếp theo TC-LOGIN-023 — sai lần 2 trên cùng tài khoản `{{lockout_email_2}}`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_2}}", password: "SAI2-again" },
    expectedStatus: 401, expectedBody: "401 — lock đặt ngầm, response vẫn 401",
    basis: "FR-02", source: "AI",
    test: `pm.test("TC-LOGIN-023b: sai lan 2", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-023c", technique: "State", partition: "tiếp theo TC-LOGIN-023b — request thứ 3 (dùng ĐÚNG mật khẩu) PHẢI bị 403 vì lock đã đặt",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_2}}", password: "RightPass1!" },
    expectedStatus: 403, expectedBody: "403 `{error:\"Tài khoản đã bị khóa. Vui lòng thử lại sau.\"}`",
    basis: "FR-02: khóa tạm 30 giây sau đủ số lần sai", source: "AI",
    test: `pm.test("TC-LOGIN-023c: request thu 3 - bi khoa du dung mat khau", () => {\n  pm.response.to.have.status(403);\n  pm.expect(pm.response.json().error).to.include("khóa");\n});`,
  },
  {
    id: "TC-LOGIN-024a", technique: "State", partition: "tài khoản thứ ba — bước 0: đăng ký",
    method: "POST", path: "/api/register", auth: "không có header",
    body: { name: "State Lockout 3", email: "{{lockout_email_3}}", password: "RightPass1!" },
    expectedStatus: 200, expectedBody: "`{message, id}`", basis: "spec §1.1", source: "AI",
    preRequest: `pm.environment.set("lockout_email_3", "hw06-lockout3-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-024a: dang ky tai khoan 3", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-LOGIN-024", technique: "State", partition: "tài khoản thứ ba — 1 lần sai (chưa khóa) rồi đăng nhập ĐÚNG → bộ đếm phải reset về 0",
    method: "POST", path: "/api/login", auth: "không có header",
    bodyDesc: "sai #1 (401) → đúng (200, reset attempts=0) → verify: sai thêm 1 lần vẫn CHỈ 401 (không cộng dồn từ trước)",
    body: { email: "{{lockout_email_3}}", password: "SAI" },
    expectedStatus: 401, expectedBody: "401 — bước 1/3 của chuỗi reset",
    basis: "FR-02", source: "AI",
    test: `pm.test("TC-LOGIN-024 buoc 1: sai lan 1", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-024b", technique: "State", partition: "tiếp theo TC-LOGIN-024 — đăng nhập ĐÚNG ngay sau 1 lần sai → phải reset bộ đếm",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_3}}", password: "RightPass1!" },
    expectedStatus: 200, expectedBody: "200 — đăng nhập đúng thành công, bộ đếm phải reset về 0",
    basis: "FR-02 (reset khi đăng nhập đúng)", source: "AI",
    test: `pm.test("TC-LOGIN-024b: dang nhap dung - reset bo dem", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-LOGIN-024c", technique: "State", partition: "tiếp theo TC-LOGIN-024b — sai 1 lần NỮA sau khi đã reset → phải CHỈ là 401 (không cộng dồn từ lần trước khi reset)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_3}}", password: "SAI-again" },
    expectedStatus: 401, expectedBody: "401 — nếu bộ đếm không reset đúng thì đây sẽ vô tình bị khóa oan",
    basis: "FR-02", source: "AI",
    test: `pm.test("TC-LOGIN-024c: sau reset - sai 1 lan van chi 401", () => { pm.response.to.have.status(401); });`,
  },

  // ============================= SECURITY (8 case) =============================
  {
    id: "TC-LOGIN-025", technique: "Security", sec: "SEC-05",
    partition: "SQLi tautology trong `email` — `' OR '1'='1`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "' OR '1'='1", password: "x" }, expectedStatus: 401,
    expectedBody: "401, KHÔNG bypass, KHÔNG 500 — parameterized query",
    basis: "SEC-05", source: "AI",
    test: `pm.test("TC-LOGIN-025 SEC-05: SQLi tautology bi chan", () => {\n  pm.response.to.have.status(401);\n  pm.expect(pm.response.json()).to.not.have.property("token");\n});`,
  },
  {
    id: "TC-LOGIN-026", technique: "Security", sec: "SEC-05",
    partition: "SQLi comment — `admin@eshop.com'--`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "admin@eshop.com'--", password: "x" }, expectedStatus: 401,
    expectedBody: "401, KHÔNG bypass", basis: "SEC-05", source: "AI",
    test: `pm.test("TC-LOGIN-026 SEC-05: SQLi comment bi chan", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-027", technique: "Security", sec: "SEC-05",
    partition: "SQLi UNION SELECT",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "x' UNION SELECT 1,2,3,4,5,6,7,8,9--", password: "x" }, expectedStatus: 401,
    expectedBody: "401, KHÔNG 500, KHÔNG lộ dữ liệu bảng khác", basis: "SEC-05", source: "AI",
    test: `pm.test("TC-LOGIN-027 SEC-05: SQLi UNION bi chan", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-028", technique: "Security", sec: "SEC-01",
    partition: "response đăng nhập thành công KHÔNG được chứa `password`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!" }, expectedStatus: 200,
    expectedBody: "`user` KHÔNG có field `password`",
    basis: "SEC-01: mật khẩu không được lưu/lộ dạng plaintext", source: "AI",
    test: `pm.test("TC-LOGIN-028 SEC-01: khong lo password trong response", () => {\n  pm.response.to.have.status(200);\n  pm.expect(pm.response.json().user, "user").to.not.have.property("password");\n});`,
  },
  {
    id: "TC-LOGIN-029", technique: "Security", sec: "SEC-01",
    partition: "response KHÔNG được chứa các cột nội bộ `login_attempts`, `locked_until`, `reset_token`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!" }, expectedStatus: 200,
    expectedBody: "`user` không có 3 field nội bộ trên",
    basis: "nguyên tắc không rò rỉ dữ liệu nội bộ — hệ quả của SEC-01/SEC-02", source: "AI",
    test: `pm.test("TC-LOGIN-029 SEC-01: khong lo cot noi bo", () => {\n  const u = pm.response.json().user;\n  ["login_attempts","locked_until","reset_token"].forEach((f) =>\n    pm.expect(u, f).to.not.have.property(f));\n});`,
  },
  {
    id: "TC-LOGIN-030", technique: "Security", sec: "SEC-02",
    partition: "JWT phải có claim `exp` (thời hạn sử dụng)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!" }, expectedStatus: 200,
    expectedBody: "token decode được, có claim `exp` ở tương lai gần",
    basis: "SEC-02: yêu cầu JWT Token hợp lệ — hợp lệ bao gồm có thời hạn", source: "AI",
    test: `pm.test("TC-LOGIN-030 SEC-02: JWT phai co exp", () => {\n  const tok = pm.response.json().token;\n  const payload = JSON.parse(Buffer.from(tok.split(".")[1].replace(/-/g,"+").replace(/_/g,"/"), "base64").toString("utf8"));\n  pm.expect(payload, "claim exp").to.have.property("exp");\n});`,
  },
  {
    id: "TC-LOGIN-031a", technique: "Security", sec: "(chuẩn bị nạn nhân)",
    partition: "bước 0: đăng ký tài khoản 'nạn nhân' cho kịch bản DoS-lockout",
    method: "POST", path: "/api/register", auth: "không có header",
    body: { name: "Victim", email: "{{lockout_email_4}}", password: "MatKhauThatCuaNanNhan!" },
    expectedStatus: 200, expectedBody: "`{message, id}`", basis: "spec §1.1", source: "AI",
    preRequest: `pm.environment.set("lockout_email_4", "hw06-dos-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-031a: dang ky tai khoan nan nhan", () => { pm.response.to.have.status(200); });`,
  },
  {
    id: "TC-LOGIN-031", technique: "Security", sec: "SEC-02 (DoS qua cơ chế khóa)",
    partition: "khóa được tài khoản NGƯỜI KHÁC chỉ bằng cách gửi body THIẾU `password` nhiều lần",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_4}}" },
    expectedStatus: 401, expectedBody: "401 lần 1 — bước dựng chuỗi tấn công DoS lockout",
    basis: "hệ quả bất lợi của FR-02 khi không kiểm `password` tồn tại trước khi cộng bộ đếm", source: "AI",
    test: `pm.test("TC-LOGIN-031: DoS lockout - buoc 1 (thieu password)", () => { pm.response.to.have.status(401); });`,
  },
  {
    id: "TC-LOGIN-032", technique: "Security", sec: "SEC-01",
    partition: "endpoint không có `Authorization` vẫn phải hoạt động bình thường (public endpoint)",
    method: "POST", path: "/api/login", auth: "token rác",
    extraHeaders: { Authorization: "Bearer token-rac-khong-hop-le" },
    body: { email: "test@eshop.com", password: "Test1234!" }, expectedStatus: 200,
    expectedBody: "200 — endpoint login không cần và không được ảnh hưởng bởi header Authorization gửi kèm",
    basis: "spec §1.2 không đòi Authorization cho login", source: "AI",
    test: `pm.test("TC-LOGIN-032: header Authorization thua khong anh huong", () => { pm.response.to.have.status(200); });`,
  },

  // ============================= SCHEMA (8 case) =============================
  {
    id: "TC-LOGIN-033", technique: "Schema", partition: "response thành công: đủ field & đúng kiểu",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!" }, expectedStatus: 200,
    expectedBody: "`message` string, `token` string, `user` object, `user.id` number, `user.email` string",
    basis: "spec §1.2", source: "AI",
    test: `pm.test("TC-LOGIN-033: schema thanh cong", () => {\n  const b = pm.response.json();\n  pm.expect(b.message).to.be.a("string");\n  pm.expect(b.token).to.be.a("string");\n  pm.expect(b.user).to.be.an("object");\n  pm.expect(b.user.id).to.be.a("number");\n  pm.expect(b.user.email).to.be.a("string");\n});`,
  },
  {
    id: "TC-LOGIN-034", technique: "Schema", partition: "response lỗi 401: đúng shape `{error: string}`",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "SAI" }, expectedStatus: 401,
    expectedBody: "chỉ có field `error`, kiểu string, `Content-Type: application/json`",
    basis: "nguyên tắc API trả JSON nhất quán", source: "AI",
    test: `pm.test("TC-LOGIN-034: schema loi 401", () => {\n  pm.response.to.have.header("Content-Type");\n  pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");\n  const b = pm.response.json();\n  pm.expect(b.error).to.be.a("string");\n  pm.expect(Object.keys(b)).to.eql(["error"]);\n});`,
  },
  {
    id: "TC-LOGIN-035", technique: "Schema", partition: "response lỗi khi bị khóa (403): đúng shape, KHÔNG lộ chi tiết engine",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_2}}", password: "x" }, expectedStatus: 403,
    expectedBody: "`{error}`, Content-Type JSON, không chứa `SQLITE_`/stack trace",
    basis: "SEC-05 nguyên tắc không rò rỉ nội bộ", source: "AI",
    test: `pm.test("TC-LOGIN-035: schema loi 403", () => {\n  const t = pm.response.text();\n  ["SQLITE_", "<pre>", "node_modules"].forEach((s) => pm.expect(t).to.not.include(s));\n});`,
  },
  {
    id: "TC-LOGIN-036", technique: "Schema", partition: "body request KHÔNG phải JSON hợp lệ (`Content-Type: application/json` nhưng body là text thường)",
    method: "POST", path: "/api/login", auth: "không có header",
    extraHeaders: {}, body: "day khong phai JSON",
    expectedStatus: 400, expectedBody: "phản hồi lỗi phải là JSON `{error}`, KHÔNG được là trang HTML kèm stack trace tuyệt đối",
    basis: "nguyên tắc API luôn trả JSON, không rò rỉ đường dẫn hệ thống — suy từ SEC-05/nguyên tắc chung", source: "AI",
    test: `pm.test("TC-LOGIN-036: body khong phai JSON - khong duoc la HTML loi", () => {\n  pm.response.to.have.status(400);\n  pm.expect(pm.response.headers.get("Content-Type"), "Content-Type phai la JSON").to.include("application/json");\n});`,
  },
  {
    id: "TC-LOGIN-037", technique: "Schema", partition: "response không được lộ field thừa ngoài đặc tả",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "test@eshop.com", password: "Test1234!" }, expectedStatus: 200,
    expectedBody: "`user` chỉ gồm field đặc tả cho là cần cho client: id, name, email, role, shipping_address, phone — KHÔNG có password/login_attempts/locked_until/reset_token",
    basis: "spec §1.2 + SEC-01", source: "AI",
    test: `pm.test("TC-LOGIN-037: khong lo field thua", () => {\n  const u = pm.response.json().user;\n  const allow = ["id","name","email","role","shipping_address","phone"];\n  Object.keys(u).forEach((k) => pm.expect(allow, "field " + k + " khong duoc phep").to.include(k));\n});`,
  },
  {
    id: "TC-LOGIN-038", technique: "Schema", partition: "`Content-Type` response luôn `application/json` kể cả nhánh lỗi",
    method: "POST", path: "/api/login", auth: "không có header",
    body: {}, expectedStatus: 401, expectedBody: "Content-Type application/json",
    basis: "nguyên tắc API JSON nhất quán", source: "AI",
    test: `pm.test("TC-LOGIN-038: content-type json o nhanh loi", () => {\n  pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");\n});`,
  },
];

export const own = [
  {
    id: "TC-LOGIN-101", technique: "Security", sec: "(DoS lockout)",
    partition: "SV: DoS khóa tài khoản NGƯỜI KHÁC — kẻ tấn công gửi thêm 1 request thiếu `password` nữa (bước 2, sau bước 1 ở TC-LOGIN-031) — CHƯA phản ánh khóa ở response này (đúng mô hình đã xác nhận)",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_4}}" },
    expectedStatus: 401, expectedBody: "401 — response của chính request thứ 2 này chưa đổi, nhưng khóa đã được đặt ngầm cho request kế tiếp",
    basis: "hệ quả nguy hiểm của FR-02 khi thiếu validate — tự phát hiện khi đọc code, không phải suy từ 1 prompt", source: "SV",
    test: `pm.test("TC-LOGIN-101 SV: request thieu password lan 2 - van 401", () => {\n  pm.response.to.have.status(401);\n});`,
    why: "AI (bước 4b — Security) chỉ sinh case SQLi/XSS vì prompt yêu cầu 'security: SQL injection' — nó không tự nghĩ ra việc *thiếu* một field cũng là một vector tấn công (DoS), vì đó không khớp khuôn mẫu payload độc hại quen thuộc.",
    why_group: "prompt quality",
  },
  {
    id: "TC-LOGIN-101b", technique: "Security", sec: "(DoS lockout)",
    partition: "SV: hoàn tất chứng minh DoS — nạn nhân (chủ tài khoản thật) thử đăng nhập bằng ĐÚNG mật khẩu của mình (request thứ 3) và VẪN bị chặn, dù kẻ tấn công chưa từng biết mật khẩu",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "{{lockout_email_4}}", password: "MatKhauThatCuaNanNhan!" },
    expectedStatus: 403, expectedBody: "403 — nạn nhân bị khóa hoàn toàn dù kẻ tấn công KHÔNG hề biết mật khẩu, chỉ cần biết email",
    basis: "hệ quả nguy hiểm của FR-02 khi thiếu validate `password` tồn tại trước khi cộng bộ đếm", source: "SV",
    test: `pm.test("TC-LOGIN-101b SV: nan nhan bi khoa hoan toan boi ke khong biet mat khau", () => {\n  pm.response.to.have.status(403);\n});`,
    why: "Cùng nhóm lý do với TC-LOGIN-101 — chuỗi 2 case này là MỘT cuộc tấn công hoàn chỉnh (request 1 ở TC-LOGIN-031, request 2 ở đây, request 3 xác nhận nạn nhân bị khóa).",
    why_group: "prompt quality",
  },
  {
    id: "TC-LOGIN-102", technique: "Security", sec: "(account enumeration)",
    partition: "SV: so sánh response giữa email KHÔNG tồn tại và email TỒN TẠI + đã bị khóa — phải KHÔNG phân biệt được",
    method: "POST", path: "/api/login", auth: "không có header",
    body: { email: "khong-ton-tai-enum-test@x.com", password: "x" },
    expectedStatus: 401, expectedBody: "401 `{error:\"Invalid email or password\"}` — PHẢI KHÁC 403 mà tài khoản tồn tại+bị khóa trả về, nếu không kẻ tấn công dò được email nào có thật",
    basis: "FR-02 'không để lộ chi tiết nguyên nhân' — áp dụng chéo 2 request để lộ ra kênh ngầm (side channel)", source: "SV",
    test: `pm.test("TC-LOGIN-102 SV: enumeration - email khong ton tai luon 401", () => {\n  pm.response.to.have.status(401);\n});`,
    why: "AI chỉ kiểm 'có lộ lý do trong 1 response hay không', không nghĩ tới việc SO SÁNH 2 response với nhau mới lộ ra khác biệt trạng thái tài khoản (403 vs 401) — đây là lỗi suy luận 1-bước-1-request, không mô phỏng được góc nhìn kẻ tấn công dò nhiều tài khoản.",
    why_group: "model limitations",
  },
  {
    id: "TC-LOGIN-103", technique: "Domain", partition: "SV: đăng ký 2 tài khoản CÙNG email → tài khoản thứ 2 vĩnh viễn không đăng nhập được bằng mật khẩu của chính nó",
    method: "POST", path: "/api/login", auth: "không có header",
    bodyDesc: "2×`POST /api/register` cùng email khác mật khẩu, rồi login bằng mật khẩu của tài khoản ĐĂNG KÝ SAU",
    body: { email: "{{dup_email}}", password: "MatKhauThuHai!" },
    expectedStatus: 200, expectedBody: "200 — nếu hệ thống coi email là định danh duy nhất (ngụ ý bởi việc dùng email để login) thì tài khoản mới nhất với email đó phải đăng nhập được bằng đúng mật khẩu của nó",
    basis: "spec §1.1/§1.2 dùng email làm định danh đăng nhập duy nhất — ngụ ý email phải là duy nhất", source: "SV",
    preRequest: `pm.environment.set("dup_email", "hw06-dup-" + Date.now() + "@x.com");`,
    test: `pm.test("TC-LOGIN-103 SV: tai khoan dang ky sau phai dang nhap duoc", () => {\n  pm.response.to.have.status(200);\n});`,
    why: "characteristics of the API — không có trong đặc tả API (`api_specification.md` không nói 'email phải duy nhất'), và không nằm ở tầng logic mà nằm ở TẦNG SCHEMA CSDL (`database.js` không có `UNIQUE` trên cột `email`). AI chỉ đọc file đặc tả và mã handler, không đọc schema bảng nên không thấy được ràng buộc còn thiếu.",
    why_group: "characteristics of the API",
  },
  {
    id: "TC-LOGIN-104", technique: "Schema", partition: "SV: response lỗi khi body không phải JSON KHÔNG được là trang HTML có đường dẫn tuyệt đối của server",
    method: "POST", path: "/api/login", auth: "không có header",
    body: "khong phai JSON hop le nua",
    expectedStatus: 400, expectedBody: "body KHÔNG được chứa chuỗi đường dẫn hệ thống (`D:\\`, `node_modules`, `at Object.`)",
    basis: "nguyên tắc chung: response lỗi không được lộ cấu trúc filesystem/thư viện nội bộ của server", source: "SV",
    test: `pm.test("TC-LOGIN-104 SV: khong lo duong dan he thong khi loi parse JSON", () => {\n  const t = pm.response.text();\n  ["D:\\\\","node_modules","at Object.","<pre>"].forEach((s) =>\n    pm.expect(t, "khong duoc chua \\"" + s + "\\"").to.not.include(s));\n});`,
    why: "AI (bước 5 — Schema) chỉ kiểm shape của response THÀNH CÔNG khớp đặc tả — prompt nói 'response shape matches the spec', và đặc tả chỉ mô tả response THÀNH CÔNG. Nhánh lỗi khi body không parse được là hành vi của middleware `body-parser`, nằm hoàn toàn ngoài đặc tả API, nên AI không có cơ sở nào để tự sinh case này.",
    why_group: "prompt quality",
  },
  {
    id: "TC-LOGIN-105", technique: "State", partition: "SV: token lấy từ `POST /api/login` phải dùng được ngay cho `GET /api/users/me` (dùng token đã lưu ở bước 00-setup)",
    method: "GET", path: "/api/users/me", auth: "user (token vừa login)",
    authHeader: "Bearer {{user_token}}",
    bodyDesc: "dùng `{{user_token}}` đã lưu từ 00-setup → gọi `GET /api/users/me`",
    body: null, expectedStatus: 200, expectedBody: "200, `email` khớp tài khoản vừa login",
    basis: "spec §2.1 + §1.2 (token dùng cho các request có xác thực)", source: "SV",
    test: `pm.test("TC-LOGIN-105 SV: token dung goi duoc /users/me", () => {\n  pm.response.to.have.status(200);\n  pm.expect(pm.response.json().email).to.eql("test@eshop.com");\n});`,
    why: "AI sinh case cho từng endpoint RIÊNG LẺ theo đúng phạm vi API đã chọn (`POST /api/login`), không tự nối sang endpoint khác (`GET /api/users/me`) để xác nhận TOKEN THẬT SỰ DÙNG ĐƯỢC — đây là kiểm tra tích hợp xuyên-API mà một prompt chỉ nhắm 1 endpoint sẽ không tạo ra.",
    why_group: "prompt quality",
  },
];
